import { useEffect } from "react";
import { Pressable, StyleSheet, View } from "react-native";
import Animated, {
  useAnimatedStyle,
  useSharedValue,
  withSpring,
} from "react-native-reanimated";

type Props = {
  progress: number;
  maxProgress: number;
};

export default function ProgressBar({ progress, maxProgress }: Props) {
  

  return (
    
  );
}

const styles = StyleSheet.create({
  
});
